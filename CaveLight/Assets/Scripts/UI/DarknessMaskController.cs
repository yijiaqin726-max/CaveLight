using UnityEngine;

// Legacy placeholder kept so old serialized references do not break if a scene
// or prefab still contains the component. It intentionally does nothing.
public class DarknessMaskController : MonoBehaviour
{
}
